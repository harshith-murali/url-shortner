/*
 * PostCSS configuration.
 *
 * Tailwind CSS was removed because no Tailwind utility classes were used
 * in the codebase. The design system is implemented entirely with CSS
 * custom properties (variables) in globals.css.
 */
const config = {
  plugins: {},
};

export default config;
